# ---------- Development ----------
# Backend URL for server components inside Docker network (server-only)
NEXT_PRIVATE_API_URL=your_backend_domain

# Backend URL exposed to browser client components in development (for API calls)
NEXT_PUBLIC_API_URL=your_backend_domain
